import React from 'react';

/**
 * Componente Breadcrumb - Oculto a pedido del usuario.
 * Se mantiene el archivo exportando null para no romper las importaciones.
 */
export default function Breadcrumbs(): React.ReactElement | null {
  return null;
}
